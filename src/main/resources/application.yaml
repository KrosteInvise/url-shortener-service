spring:
  application:
    name: url-shortener-service
  jpa:
    hibernate:
      ddl-auto: update
  cache:
    type: redis
  liquibase:
    enabled: true
    change-log: classpath:db/changelog/db.changelog-master.xml